# 배송 실패 감사 타임라인 표시

영구 저장된 Shopify 배송 `FAILED` 이벤트는 주문 상세에서 빨간 실패 항목으로 표시한다. Shopify fulfillment 생성 전 발생한 실패는 ID 대신 `Shopify 생성 전 중단`으로 나타내고 원본 오류 메시지와 worker ID를 함께 제공한다.

현재 실패 카드가 수동 재시도 후 사라지더라도 영구 실패 이벤트와 이후 재시도 이력은 감사 타임라인에 계속 남는다.
