# Shopify 배송 반영 실패 조회

주문 운영 화면은 `배송 반영 실패` 필터로 실패한 `shopify_fulfillment_jobs`가 연결된 주문만 조회한다. 주문 자체의 `SUBMITTED` 상태는 변경하지 않아 Printful 제작 제출 사실을 보존한다.

주문 상세에는 실패한 배송 작업의 송장번호, 누적 시도 횟수와 마지막 오류를 표시한다. 정상 생성 및 응답 유실 복구 이벤트는 동일 상세의 감사 타임라인에서 확인한다.
