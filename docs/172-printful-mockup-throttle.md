# Printful 목업 호출 제한 복구

Printful 목업 생성과 상태 조회가 `429`를 반환하면 `Retry-After`까지 작업을 연기한다. 제한 대기는 실패가 아니므로 attempts를 차감하고, 원격 task가 이미 생성된 작업은 `WAITING_REMOTE`, 생성 전 작업은 `PENDING`으로 복원한다.

`408`과 `5xx`는 최대 시도 횟수 안에서 지수 백오프로 재시도한다. 디자인 검증 오류, 완료 task의 실패 응답, 응답 구조 오류는 운영자가 확인할 수 있도록 기존 `FAILED` 처리한다.
