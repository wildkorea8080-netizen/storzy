# 브랜드 온보딩 API 인증

워크스페이스 생성, 브랜드 프로필 생성·조회·편집·승인과 운영 알림 조회·읽음 처리는 관리자 Bearer 토큰을 요구한다. 인증은 JSON 본문 파싱과 서비스 호출 전에 실행한다.

온보딩 화면은 관리자 메인 화면에서 세션에 저장한 `storzy.adminToken`을 동일 출처 `/api/` 요청에만 전달한다. 외부 URL이나 정적 자산 요청에는 토큰을 추가하지 않는다.

이 경계는 임의 사용자의 워크스페이스 생성, AI 생성 작업 유발, 브랜드 기준 변경과 운영 알림 열람을 차단한다.
