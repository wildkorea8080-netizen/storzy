# Shopify 배송 반영 수동 재시도

배송 반영에 실패한 주문 상세에서 담당자와 사유를 입력한 뒤 재시도할 수 있다. 서버는 현재 `FAILED`인 최신 배송 job만 잠그고 `PENDING`으로 전환하며 시도 횟수를 초기화한다.

요청은 워크스페이스 범위의 멱등키로 중복 실행을 막는다. 담당자, 사유, 재시도 전 횟수는 별도 감사 테이블에 저장되고 주문 타임라인에 표시된다.
