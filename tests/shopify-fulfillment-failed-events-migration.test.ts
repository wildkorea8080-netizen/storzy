import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync(new URL("../migrations/064_shopify_fulfillment_failed_events.sql",import.meta.url),"utf8");describe("Shopify fulfillment failed event migration",()=>{it("adds durable failure detail without requiring a fulfillment id",()=>{expect(sql).toContain("'FAILED'");expect(sql).toContain("ADD COLUMN detail text");expect(sql).toContain("shopify_fulfillment_id DROP NOT NULL")})});
