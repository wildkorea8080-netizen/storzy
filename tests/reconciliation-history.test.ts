import{describe,expect,it,vi}from"vitest";import{OrderReconciliationService}from"../src/orders/shopify-reconciliation.js";
describe("reconciliation audit history",()=>{it("returns bounded issue history with aggregated actions",async()=>{const query=vi.fn().mockResolvedValue({rows:[{id:"i",status:"RESOLVED",actions:[{action:"SYNC_FINANCIAL_STATUS",actorId:"admin",reason:"verified"}]}]}),service=new OrderReconciliationService({query}as never);await expect(service.listHistory("w",500)).resolves.toMatchObject([{id:"i",actions:[{action:"SYNC_FINANCIAL_STATUS"}]}]);expect(query).toHaveBeenCalledWith(expect.stringContaining("jsonb_agg"),["w",100]);expect(query).toHaveBeenCalledWith(expect.stringContaining("order_reconciliation_issue_actions"),["w",100]);});});
