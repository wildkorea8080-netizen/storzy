import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync(new URL("../migrations/062_shopify_fulfillment_job_events.sql",import.meta.url),"utf8");describe("Shopify fulfillment event migration",()=>{it("stores created and recovered events by workspace and shipment",()=>{for(const value of ["shopify_fulfillment_job_events","workspace_id","shipment_id","CREATED","RECOVERED","shopify_fulfillment_job_event_history"]){expect(sql).toContain(value)}})});
