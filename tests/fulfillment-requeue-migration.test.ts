import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync(new URL("../migrations/063_shopify_fulfillment_requeue_actions.sql",import.meta.url),"utf8");describe("fulfillment requeue migration",()=>{it("stores idempotent workspace-scoped actions",()=>{for(const value of ["shopify_fulfillment_requeue_actions","workspace_id","commerce_order_id","idempotency_key","before_attempts","UNIQUE(workspace_id,idempotency_key)"]){expect(sql).toContain(value)}})});
