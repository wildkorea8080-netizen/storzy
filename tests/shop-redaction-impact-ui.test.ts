import{describe,expect,it}from"vitest";import{shopRedactionImpactCss,shopRedactionImpactJs}from"../src/admin/shop-redaction-impact-ui.js";
describe("shop redaction impact UI",()=>{it("shows base records and provider audit extensions before execution",()=>{for(const value of ["shop-redaction-impact","shop-redaction-audit-impact","Promise.all","printfulOrderAudits","fulfillmentAudits","fulfillmentRequeues","draftCleanupAudits","uninstallReceipts","앱 삭제 수신","prompt(","confirm("]){expect(shopRedactionImpactJs).toContain(value)}expect(shopRedactionImpactCss).toContain(".impact-audits");expect(()=>new Function(shopRedactionImpactJs)).not.toThrow()})});
