import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync(new URL("../migrations/065_shop_redaction_audit_extensions.sql",import.meta.url),"utf8");
describe("shop redaction audit extensions",()=>{it("redacts every provider audit table before completion",()=>{for(const table of ["printful_order_job_events","shopify_fulfillment_job_events","shopify_fulfillment_requeue_actions","printful_draft_cleanup_actions"]){expect(sql).toContain(`UPDATE ${table}`)}expect(sql).toContain("OLD.workspace_id");expect(sql).toContain("OLD.request_type='SHOP_REDACT'");expect(sql).toContain("NEW.status='COMPLETED'");expect(sql).toContain("BEFORE UPDATE OF status")});it("removes external identifiers and free-text provider details",()=>{expect(sql).toContain("remote_order_id=NULL,detail=NULL");expect(sql).toContain("shopify_fulfillment_id=NULL,detail=NULL");expect(sql.match(/reason='REDACTED'/g)).toHaveLength(2)})});
