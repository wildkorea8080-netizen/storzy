import{describe,expect,it}from"vitest";import{designsHtml,designsJs}from"../src/admin/designs-page.js";
describe("Admin designs page",()=>{it("supports Korean design cards, preview samples, and print-area review",()=>{expect(designsHtml).not.toContain("<script>");expect(designsHtml).toContain("미리보기 샘플 적용");expect(designsHtml).toContain("인쇄 너비");expect(designsJs).toContain("preview-assets.storzy.local/seoul-side-design.png");expect(designsJs).toContain("decisionStatus=APPROVED");expect(designsJs).toContain("/design-asset");expect(designsJs).toContain("/design-resolution/override");expect(designsJs).toContain("Idempotency-Key");expect(designsJs).toContain("GUIDELINE_MISSING");expect(designsJs).toContain("effectiveDpi");expect(designsJs).toContain("allowedMockupStyleIds")})});
