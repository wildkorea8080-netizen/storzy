import{describe,expect,it}from"vitest";import{shopifyTokenAlertCss,shopifyTokenAlertJs}from"../src/admin/shopify-token-alert-ui.js";
describe("Shopify token alert UI",()=>{it("lists failed deliveries and requests an audited requeue",()=>{expect(shopifyTokenAlertJs).toContain("shopify-token-alert-deliveries");expect(shopifyTokenAlertJs).toContain("/requeue");expect(shopifyTokenAlertJs).toContain("actorId:'admin-ui'");expect(shopifyTokenAlertJs).toContain("재시도 사유");expect(shopifyTokenAlertCss).toContain(".token-delivery");expect(()=>new Function(shopifyTokenAlertJs)).not.toThrow()})});
