import{describe,expect,it,vi}from"vitest";
import{PrivacyRequestService}from"../src/privacy/request-service.js";

describe("privacy maintenance delivery history",()=>{it("lists bounded incident actions newest first with the prior retry count",async()=>{const created=new Date("2026-08-20T12:00:00.000Z"),query=vi.fn().mockResolvedValue({rows:[{id:"action-1",delivery_id:"delivery-1",action:"REQUEUE",actor_id:"operator",reason:"운영 채널 복구",before_status:"FAILED",after_status:"PENDING",before_attempts:6,created_at:created}]}),service=new PrivacyRequestService({query}as never);await expect(service.maintenanceDeliveryActions("incident-1",500)).resolves.toEqual([{id:"action-1",deliveryId:"delivery-1",action:"REQUEUE",actorId:"operator",reason:"운영 채널 복구",beforeStatus:"FAILED",afterStatus:"PENDING",beforeAttempts:6,createdAt:created.toISOString()}]);expect(String(query.mock.calls[0]?.[0])).toContain("a.before_attempts");expect(String(query.mock.calls[0]?.[0])).toContain("ORDER BY a.created_at DESC");expect(query.mock.calls[0]?.[1]).toEqual(["incident-1",100]);});});
