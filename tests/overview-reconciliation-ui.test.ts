import{describe,expect,it}from"vitest";import{overviewReconciliationJs}from"../src/admin/overview-reconciliation-ui.js";
describe("overview reconciliation alerts",()=>{it("adds issue, scanner failure, and missing schedule labels",()=>{expect(overviewReconciliationJs).toContain("counts.reconciliation");expect(overviewReconciliationJs).toContain("주문 상태 대조");expect(overviewReconciliationJs).toContain("주문 대조 실행");expect(overviewReconciliationJs).toContain("RECONCILIATION_RUN_STALLED");expect(overviewReconciliationJs).toContain("RECONCILIATION_RUN_NEVER");expect(overviewReconciliationJs).toContain("RECONCILIATION_RUN_OVERDUE");expect(overviewReconciliationJs).toContain("26시간");expect(overviewReconciliationJs).toContain("FINANCIAL_STATUS_MISMATCH");expect(overviewReconciliationJs).toContain("/admin/orders");expect(overviewReconciliationJs).toContain("tabindex");expect(()=>new Function(overviewReconciliationJs)).not.toThrow();});});
