import {describe,expect,it} from "vitest";import {orderReturnsCss,orderReturnsJs} from "../src/admin/order-returns-ui.js";
describe("returned order UI",()=>{it("adds a returned filter and dedicated resolution controls",()=>{expect(orderReturnsJs).toContain("option.value='RETURNED'");expect(orderReturnsJs).toContain("REQUIRE_REFUND");expect(orderReturnsJs).toContain("REVIEW_RESHIPMENT");expect(orderReturnsJs).toContain("RESOLVE");expect(orderReturnsJs).toContain("'Idempotency-Key':crypto.randomUUID()");expect(orderReturnsCss).toContain(".drawer.returned .actions{display:none}");expect(()=>new Function(orderReturnsJs)).not.toThrow();});});
