import{describe,expect,it}from"vitest";import{storeHtml,storeJs}from"../src/admin/store-page.js";
describe("Admin store configuration page",()=>{it("renders template, palette, page cards, and advanced editing actions",()=>{expect(storeHtml).not.toContain("<script>");expect(storeHtml).toContain("승인 브랜드에서 구성 생성");expect(storeHtml).toContain("고급 JSON 편집");expect(storeHtml).toContain('id="palette"');expect(storeHtml).toContain('id="pages"');expect(storeJs).toContain("Object.entries(c.pages");expect(storeJs).toContain("/store-drafts");expect(storeJs).toContain("/revisions");expect(storeJs).toContain("/approve");expect(storeJs).toContain("/publication/requeue")})});
