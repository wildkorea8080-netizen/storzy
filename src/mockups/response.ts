export type MockupImage=Readonly<{catalogVariantId:string;placement:string;styleId:number;url:string}>;
function records(value:unknown):Record<string,unknown>[]{return Array.isArray(value)?value.filter((x):x is Record<string,unknown>=>!!x&&typeof x==='object'&&!Array.isArray(x)):[];}
export function parseCreatedTaskIds(value:unknown):string[]{const root=value as Record<string,unknown>;return records(root?.data).map(x=>String(x.id??'')).filter(Boolean);}
export function parseMockupTasks(value:unknown):Readonly<{id:string;status:string;failureReasons:unknown[];images:MockupImage[]}[]> {const root=value as Record<string,unknown>;return records(root?.data).map(task=>{const images:MockupImage[]=[];for(const variant of records(task.catalog_variant_mockups)){for(const mockup of records(variant.mockups)){const url=String(mockup.mockup_url??'');if(/^https:\/\//.test(url))images.push({catalogVariantId:String(variant.catalog_variant_id),placement:String(mockup.placement??''),styleId:Number(mockup.style_id),url});}}return{id:String(task.id??''),status:String(task.status??''),failureReasons:Array.isArray(task.failure_reasons)?task.failure_reasons:[],images};});}
