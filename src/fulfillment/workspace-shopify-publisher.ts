import{ShopifyAdminClient}from"../integrations/shopify.js";import type{IntegrationConnectionRepository}from"../integrations/connection-repository.js";import type{ShopifyWorkspaceAccess}from"../integrations/shopify-access-token-provider.js";import{ShopifyConnectionUnavailableError}from"../shopify/workspace-publisher.js";import{ShopifyFulfillmentPublisher}from"./shopify-publisher.js";import type{FulfillmentContext}from"./shopify-job-store.js";
export type FulfillmentResult=Awaited<ReturnType<ShopifyFulfillmentPublisher["publish"]>>;export interface FulfillmentPublisher{publish(context:FulfillmentContext,workspaceId?:string):Promise<FulfillmentResult>}
export class WorkspaceShopifyFulfillmentPublisher implements FulfillmentPublisher{constructor(private readonly connections:IntegrationConnectionRepository|undefined,private readonly apiVersion:string,private readonly fallback?:Readonly<{shopDomain:string;accessToken:string}>,private readonly accessProvider?:Readonly<{resolve(workspaceId:string):Promise<ShopifyWorkspaceAccess|null>}>){ }async publish(context:FulfillmentContext,workspaceId?:string):Promise<FulfillmentResult>{if(this.accessProvider&&workspaceId){const access=await this.accessProvider.resolve(workspaceId);if(access)return new ShopifyFulfillmentPublisher(new ShopifyAdminClient({...access,apiVersion:this.apiVersion})).publish(context);throw new ShopifyConnectionUnavailableError()}else if(this.connections&&workspaceId){const connection=(await this.connections.list(workspaceId)).find(item=>item.provider==="SHOPIFY"&&item.status==="CONNECTED"),credentials=await this.connections.credentials(workspaceId,"SHOPIFY");if(connection&&credentials?.accessToken)return new ShopifyFulfillmentPublisher(new ShopifyAdminClient({shopDomain:connection.accountLabel,accessToken:credentials.accessToken,apiVersion:this.apiVersion})).publish(context)}if(this.fallback)return new ShopifyFulfillmentPublisher(new ShopifyAdminClient({...this.fallback,apiVersion:this.apiVersion})).publish(context);throw new ShopifyConnectionUnavailableError()}}
