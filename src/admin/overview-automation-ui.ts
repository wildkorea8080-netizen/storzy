export const overviewAutomationCss=`.automation-overview{border-left:5px solid #d69821}.automation-overview.enabled{border-left-color:#4d8c35}.automation-overview-head{display:flex;justify-content:space-between;gap:16px;align-items:start}.automation-overview-state{padding:7px 11px;border-radius:999px;background:#fff0cf;color:#765000;font-size:12px;font-weight:900}.automation-overview.enabled .automation-overview-state{background:#e1f4da;color:#27652b}.automation-overview-blockers{display:flex;gap:7px;flex-wrap:wrap;margin-top:13px}.automation-overview-blockers a{padding:6px 9px;border-radius:8px;background:#f4f2e8;color:#5f5b50;text-decoration:none;font-size:12px}.automation-overview-meta{margin:10px 0 0;color:#706d65;font-size:12px}`;
export const overviewAutomationJs=`(()=>{const root=document.querySelector('#automation-overview');if(!root)return;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){const workspace=(document.querySelector('#workspace')?.value||sessionStorage.getItem('storzy.workspace')||'').trim(),token=(document.querySelector('#token')?.value||sessionStorage.getItem('storzy.adminToken')||'').trim();if(!workspace){root.querySelector('.automation-overview-body').textContent='워크스페이스를 연결해 주세요.';return}const response=await fetch('/api/workspaces/'+encodeURIComponent(workspace)+'/order-automation',{headers:{...(token?{Authorization:'Bearer '+token}:{})}}),body=await response.json().catch(()=>({}));if(!response.ok){root.querySelector('.automation-overview-body').textContent=body.error?.message||'상태를 확인하지 못했습니다.';return}const data=body.data,blockers=data.approval?.blockers||[],latest=data.history?.[0];root.classList.toggle('enabled',data.enabled);root.querySelector('.automation-overview-state').textContent=data.enabled?'자동 제출 승인됨':'자동 제출 중지';root.querySelector('.automation-overview-body').textContent=data.enabled?'정책 검증을 통과한 Shopify 주문이 Printful로 자동 전달됩니다.':blockers.length?'출시 승인 전에 아래 조건을 완료해야 합니다.':'필수 조건을 충족했으며 운영 책임자 승인만 남았습니다.';root.querySelector('.automation-overview-blockers').innerHTML=blockers.map(item=>'<a href="'+item.href+'">'+esc(item.label)+'</a>').join('');root.querySelector('.automation-overview-meta').textContent=latest?'최근 변경 · '+latest.actorId+' · '+latest.reason+' · '+new Date(latest.createdAt).toLocaleString('ko-KR'):'아직 승인 또는 중지 이력이 없습니다.'}document.querySelector('#connect')?.addEventListener('click',()=>setTimeout(load));document.querySelector('#refresh')?.addEventListener('click',load);if(sessionStorage.getItem('storzy.workspace'))load()})();`;
