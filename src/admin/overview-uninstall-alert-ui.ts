export const overviewUninstallAlertCss=`.shopify-uninstall-action{margin-left:auto;align-self:center;padding:8px 11px;border-radius:9px;background:#181815;color:#fff;text-decoration:none;font-size:12px;font-weight:800}`;
export const overviewUninstallAlertJs=`(()=>{function enhance(){document.querySelectorAll('#alerts .alert').forEach(row=>{const kind=row.querySelector('.alert-kind');if(kind?.textContent!=='SHOPIFY_UNINSTALL'||row.dataset.uninstallReady)return;row.dataset.uninstallReady='1';kind.textContent='Shopify 앱 삭제';const status=row.querySelector('.alert-status');if(status)status.textContent='연결 해제';const details=row.querySelectorAll('.muted');if(details[0])details[0].textContent='Shopify에서 앱이 삭제되어 자격 증명과 주문 자동화가 중지되었습니다.';const action=document.createElement('a');action.className='shopify-uninstall-action';action.href='/admin/integrations';action.textContent='Shopify 다시 연결';row.append(action)})}new MutationObserver(()=>queueMicrotask(enhance)).observe(document.getElementById('alerts'),{childList:true,subtree:true});enhance()})();`;
