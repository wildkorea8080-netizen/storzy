export const storefrontResilienceCss=`.network-banner{position:sticky;top:0;z-index:45;padding:11px 18px;text-align:center;background:#fff0bd;color:#5b4300;font-size:13px;font-weight:800}.network-banner[hidden]{display:none}.storefront-loading{max-width:900px;margin:70px auto;padding:24px;background:transparent;color:inherit}.loading-line{height:18px;margin:14px 0;border-radius:99px;background:linear-gradient(90deg,#0001 25%,#0002 50%,#0001 75%);background-size:200% 100%;animation:storefront-loading 1.4s infinite}.loading-line:first-child{width:55%;height:42px}.loading-line:last-child{width:72%}.retry-button{margin-top:14px;border:0;border-radius:99px;padding:12px 18px;background:var(--primary,#181815);color:var(--secondary,#fff);font-weight:800;cursor:pointer}@keyframes storefront-loading{to{background-position:-200% 0}}@media(prefers-reduced-motion:reduce){.loading-line{animation:none}}`;

export const storefrontResilienceJs=`(()=>{const banner=document.createElement('div');banner.className='network-banner';banner.hidden=true;banner.setAttribute('role','status');document.body.prepend(banner);function network(){banner.hidden=navigator.onLine;banner.textContent=navigator.onLine?'':'인터넷 연결이 끊겼습니다. 연결 후 다시 시도해 주세요.'}function retry(error){if(!error||error.classList.contains('storefront-loading')||error.querySelector('.retry-button'))return;const button=document.createElement('button');button.className='retry-button';button.type='button';button.textContent='다시 시도';button.onclick=()=>location.reload();error.append(button)}window.addEventListener('online',network);window.addEventListener('offline',network);network();new MutationObserver(()=>retry(document.querySelector('#app > .error'))).observe(document.getElementById('app'),{childList:true,subtree:true});setTimeout(()=>{const loading=document.querySelector('#app > .storefront-loading');if(!loading)return;loading.classList.remove('storefront-loading');loading.innerHTML='<b>스토어를 불러오는 데 시간이 걸리고 있습니다.</b><p>네트워크 연결을 확인한 뒤 다시 시도해 주세요.</p>';retry(loading)},8000)})();`;
