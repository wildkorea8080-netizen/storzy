export const privacyExportCss=`.export-button{background:#173f70!important;color:#fff!important;border-color:#173f70!important}`;
export const privacyExportJs=`(()=>{function enhance(){document.querySelectorAll('#grid .card').forEach(card=>{if(card.dataset.exportReady)return;const title=card.querySelector('b')?.textContent,status=card.querySelector('.badge')?.textContent,start=card.querySelector('[data-start]');if(title!=='고객 데이터 제공 요청'||status!=='검토 중'||!start)return;card.dataset.exportReady='1';const button=document.createElement('button');button.className='export-button';button.textContent='고객 데이터 JSON 생성';button.onclick=async()=>{button.disabled=true;try{const token=(document.querySelector('#token')?.value||sessionStorage.getItem('storzy.adminToken')||'').trim(),id=start.dataset.start,r=await fetch('/api/admin/privacy-requests/'+id+'/export',{method:'POST',headers:{'Content-Type':'application/json',...(token?{Authorization:'Bearer '+token}:{})},body:JSON.stringify({actorId:'admin-ui'})});if(!r.ok){const b=await r.json().catch(()=>({}));throw new Error(b.error?.message||'내보내기에 실패했습니다.')}const blob=await r.blob(),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='storzy-customer-data-'+id+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}catch(error){alert(error.message)}finally{button.disabled=false}};card.querySelector('.actions')?.append(button)})}new MutationObserver(()=>queueMicrotask(enhance)).observe(document.getElementById('grid'),{childList:true,subtree:true});enhance()})();`;
